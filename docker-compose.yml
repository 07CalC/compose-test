services:
  api:
    build: ./api
    ports:
      - "3000:3000"
    environment:
      - PORT
      - SERVICE_NAME
      - SHARED_SECRET

  worker:
    build: ./worker
    depends_on:
      - api
    environment:
      - API_URL
      - SERVICE_NAME
